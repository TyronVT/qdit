# Rust / Soroban
target/
**/*.rs.bk

# Node
node_modules/
.next/
out/
build/
.turbo/

# Env
.env
.env.local
.env.*.local
!.env.example

# Supabase local stack
.branches/
.temp/

# Claude Code — local agent config and notes, at any depth.
# AGENTS.md is deliberately NOT here: it is tool-agnostic and belongs to the repo.
.claude/
.claude.json
.claude.json.backup
CLAUDE.md
CLAUDE.local.md

# OS / editor
.DS_Store
*.pem
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Pitch deck — the shared copy lives in Google Slides, linked from the README.
# These are local exports, regenerated rather than tracked.
docs/qdit-pitch-deck.pptx
docs/qdit-pitch-deck.pdf

# Video and deck exports — shared as links, never tracked.
*.mp4
*.mov
*.pptx
*.key
